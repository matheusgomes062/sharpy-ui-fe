export default interface AboutUsProps {
  sectionTitle: string;
  mode: "light" | "dark";
  primary: boolean;
  label: string;
  genericTitle: string;
  shortDescription: string;
  hasIcon: boolean;
}