export default interface IStatisticBlock {
  /**
 * Statistic is an array with 3 position where each position represents one statistic with the value (number) and the description (string)
 */
  statistic: [[number, string], [number, string], [number, string]];
}